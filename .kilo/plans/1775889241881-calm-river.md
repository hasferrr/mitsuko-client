# Plan: Remaining Redundant shadcn/ui Style Cleanup (Pass 2)

Branch: `refactor/shadcn-defaults-pass2` (create from current `refactor/shadcn-default-styles-v2`)

## Summary of Previous Work (8 commits already done)

1. `c3b8229` — Removed redundant border/bg on Card
2. `ae12a4e` — Replaced manual card divs with Card/CardContent (transcribe/ only)
3. `ae38482` — Removed redundant padding on CardContent/Header/Footer
4. `d98b399` — Used Button size variants instead of manual h/px/mr overrides
5. `5f6a882` — Removed redundant Dialog styles
6. `8247676` — Removed redundant h/p overrides on Input/Textarea/SelectTrigger
7. `f363821` — Standardized gap values and page-level spacing
8. `b703e36` — Replaced hardcoded colors with design tokens (4 pricing files)

## New Findings

### Category 1: Remove redundant `size-8` from `size="icon"` Button

Button `size="icon"` already provides `size-8`. Adding `className="size-8"` is redundant.

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `batch/populate-context-dialog.tsx` | 224 | `size="icon" className="size-8"` | Remove `size-8` from className |
| 2 | `batch/populate-context-dialog.tsx` | 239 | `size="icon" className="size-8"` | Remove `size-8` from className |
| 3 | `batch/populate-context-dialog.tsx` | 273 | `size="icon" className="size-8"` | Remove `size-8` from className |
| 4 | `batch/populate-context-dialog.tsx` | 294 | `size="icon" className="size-8"` | Remove `size-8` from className |
| 5 | `settings/few-shot-input.tsx` | 224 | `size="icon" className="size-8 text-muted-foreground hover:text-destructive"` | Remove `size-8`, keep other classes |

### Category 2: Fix contradictory Button sizing overrides

**2a: `size="sm"` + `size-8 p-0` → should use `size="icon"`**

These Buttons use `size="sm"` (h-7) but override with `size-8` (32px) and `p-0`, making `size="sm"` pointless. They contain single characters (`-`, `+`) and should use `size="icon"`.

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `batch/batch-translation-view.tsx` | 497-503 | `variant="ghost" size="sm" className="size-8 flex items-center justify-center p-0 hover:text-foreground text-lg font-medium select-none"` | `variant="ghost" size="icon" className="hover:text-foreground text-lg font-medium select-none"` |
| 2 | `batch/batch-translation-view.tsx` | 516-522 | Same pattern | Same fix |
| 3 | `batch/batch-extraction-view.tsx` | 579-585 | Same pattern | Same fix |
| 4 | `batch/batch-extraction-view.tsx` | 600-606 | Same pattern | Same fix |

**2b: `size="sm"` + `h-8 px-2` → should use default size (h-8)**

`size="sm"` gives h-7, but `h-8` overrides to default height, and `px-2` overrides sm's `px-2.5`. This is contradictory — just use default size and remove overrides.

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `settings/custom-instructions-save-dialog.tsx` | 35-40 | `size="sm" className="h-8 px-2"` | Remove `size="sm"`, `h-8 px-2` |
| 2 | `settings/context-document-input.tsx` | 56-61 | `size="sm" className="h-8 px-2"` | Remove `size="sm"`, `h-8 px-2` |
| 3 | `settings/custom-instructions-input.tsx` | 53-55 | `size="sm" className="h-8 px-2"` | Remove `size="sm"`, `h-8 px-2` |
| 4 | `settings/custom-instructions-library-controls.tsx` | 44-46 | `size="sm" className="h-8 px-2"` | Remove `size="sm"`, `h-8 px-2` |
| 5 | `transcribe/settings-transcription.tsx` | 135-137 | `size="sm" className="h-8 px-2"` | Remove `size="sm"`, `h-8 px-2` |

**2c: `variant="ghost"` + `size-8 p-0` → should use `size="icon"`**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `dashboard/project-item.tsx` | 116-119 | `variant="ghost" className="size-8 p-0 hover:bg-accent"` | `variant="ghost" size="icon" className="hover:bg-accent"` |

**2d: `ml-1` on span inside Button (redundant — Button gap handles spacing)**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `transcribe/settings-transcription.tsx` | 140 | `<span className="ml-1">Presets</span>` | Remove `ml-1` |

### Category 3: Remove redundant `border-border` from outline Button variant

Button `variant="outline"` already includes `border-border` in its CVA definition. Adding it again is redundant.

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `transcribe/transcription-result-panel.tsx` | 94 | `className="text-xs border-border"` | `className="text-xs"` |
| 2 | `transcribe/transcription-result-panel.tsx` | 119 | `cn("text-xs border-border", isEditing && "border-primary/50")` | `cn("text-xs", isEditing && "border-primary/50")` |
| 3 | `transcribe/transcription-result-panel.tsx` | 131 | `className="text-xs border-border"` | `className="text-xs"` |
| 4 | `transcribe/transcription-result-panel.tsx` | 190 | `className="text-xs border-border"` | `className="text-xs"` |
| 5 | `transcribe/transcription-result-panel.tsx` | 215 | `className="text-xs border-border"` | `className="text-xs"` |
| 6 | `transcribe/transcription-result-panel.tsx` | 223 | `className="text-xs border-border"` | `className="text-xs"` |
| 7 | `transcribe/whisper-settings-panel.tsx` | 150 | `className="border-border"` | Remove className entirely or leave empty string |
| 8 | `transcribe/whisper-settings-panel.tsx` | 162 | `className="border-border"` | Remove className entirely or leave empty string |
| 9 | `transcribe/transcription-next-actions.tsx` | 43 | `className="w-full mt-2 border-border"` | `className="w-full mt-2"` |
| 10 | `transcribe/transcription-next-actions.tsx` | 64 | `className="w-full mt-2 border-border"` | `className="w-full mt-2"` |

### Category 4: Fix non-functional `hover:border-primary` on Card

Card uses `ring-1 ring-foreground/10`, not `border`. Therefore:
- `hover:border-primary` on Card is non-functional (no `border-width` is set)
- `border-primary`/`border-muted` on Card is non-functional
- Adding `border` to Card creates a double-outline (ring + border)

Fix: Change `border` → remove (Card already has ring), change `hover:border-primary` → `hover:ring-primary`, change `border-primary`/`border-muted` → `ring-primary`/`ring-foreground/10`.

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `project/project.tsx` | 147 | `"cursor-pointer hover:border-primary transition-colors overflow-hidden h-full flex flex-col"` | `"cursor-pointer hover:ring-primary transition-colors overflow-hidden h-full flex flex-col"` |
| 2 | `batch/sortable-batch-card.tsx` | 30 | `"cursor-pointer hover:border-primary transition-colors overflow-hidden h-full flex flex-col"` | `"cursor-pointer hover:ring-primary transition-colors overflow-hidden h-full flex flex-col"` |
| 3 | `library/import-instructions-dialog.tsx` | 158-159 | `"overflow-hidden border h-full flex flex-col..."` + `selectedImportIds.has(item.id) ? "border-primary" : "border-muted"` | Remove `border`, change `border-primary` → `ring-primary`, change `border-muted` → `ring-foreground/10` |
| 4 | `library/my-library.tsx` | ~220 | `selectedIds.has(item.id) && "border-primary"` | `selectedIds.has(item.id) && "ring-primary"` |

### Category 5: Remove redundant padding overrides on CardContent/CardFooter

**5a: CardContent with full padding override (replaces base `px-4`)**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `pricing/credit-usage.tsx` | 63 | `className="p-8"` | Remove — Card provides `py-4 gap-4`, CardContent provides `px-4`. Use `space-y-*` on children instead. Note: content currently uses `mb-4`, `mb-6` on children which should become `space-y-4` on CardContent. |
| 2 | `pricing/credit-pack-prices.tsx` | 111 | `className="p-6"` | Remove — same reasoning. |

**5b: CardContent with partial vertical padding override**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 3 | `pricing/credit-pack-prices.tsx` | 158 | `className="pt-2 pb-4"` | Remove — Card provides `py-4 gap-4` which handles vertical spacing between CardHeader and CardContent. |

**5c: CardFooter with padding override**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 4 | `pricing/credit-pack-prices.tsx` | 182 | `className="pt-0"` | Remove — CardFooter provides `p-4`, and Card has `has-[card-footer]:pb-0` |
| 5 | `library/public-library.tsx` | 294 | `className="pt-2 flex justify-between..."` | Remove `pt-2`, keep rest — CardFooter base `p-4` handles padding |
| 6 | `library/my-library.tsx` | 246 | `className="pt-2 flex justify-end gap-2"` | Remove `pt-2`, keep rest |

**5d: Redundant `text-card-foreground` inside Card**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 7 | `pricing/credit-usage.tsx` | 64 | `text-card-foreground` on `<h3>` | Remove — Card already provides `text-card-foreground` to all descendants |

### Category 6: Remove redundant mt/mb inside space-y/gap parents

When a parent has `space-y-*` or `gap-*`, per-child `mt-*`/`mb-*` is either overridden or creates double-spacing.

**6a: `mt-3` inside `space-y-4` (overridden, dead code)**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `batch/batch-translation-view.tsx` | 488 | `className="mt-3 flex items-center justify-between"` | Remove `mt-3` |
| 2 | `batch/batch-extraction-view.tsx` | 569 | `className="mt-3 flex items-center justify-between"` | Remove `mt-3` |
| 3 | `batch/batch-extraction-view.tsx` | 612 | `className="mt-3 flex items-center justify-between"` | Remove `mt-3` |

**6b: `mt-2`/`mt-4` inside `space-y-4` CardContent**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 4 | `transcribe/transcription-select-tab.tsx` | 158 | `className="... mt-2"` | Remove `mt-2` |
| 5 | `transcribe/transcription-select-tab.tsx` | 175 | `className="... mt-4"` | Remove `mt-4` |

**6c: `mb-2` inside `space-y-2` parents**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 6 | `settings/temperature-slider.tsx` | 24 | `className="flex justify-between mb-2 items-center"` | Remove `mb-2` |
| 7 | `settings/split-size-input.tsx` | 34 | `className="flex justify-between mb-2 items-center"` | Remove `mb-2` |
| 8 | `settings/max-completion-token-input.tsx` | 45 | `className="flex justify-between mb-2 items-center"` | Remove `mb-2` |
| 9 | `settings/start-index-input.tsx` | 60 | `className="flex justify-between mb-2 items-center"` | Remove `mb-2` |
| 10 | `settings/end-index-input.tsx` | 44 | `className="flex justify-between mb-2 items-center"` | Remove `mb-2` |

**6d: `mb-4` inside DialogContent `gap-4`**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 11 | `settings/transcription-settings-dialogue.tsx` | 110 | `className="... mb-4 ..."` | Remove `mb-4` |
| 12 | `settings/settings-dialogue.tsx` | 154 | `className="... mb-4 ..."` | Remove `mb-4` |

**6e: `mb-1` inside CardHeader (which has gap-1)**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 13 | `pricing/credit-pack-prices.tsx` | 132 | `className="... mb-1"` | Remove `mb-1` |

**6f: `mb-3` inside `space-y-4`**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 14 | `pricing/payment-options-dialog.tsx` | 260 | `className="... mb-3"` | Remove `mb-3` |
| 15 | `pricing/payment-options-dialog.tsx` | 333 | `className="... mb-3"` | Remove `mb-3` |

### Category 7: Remove redundant `gap-2` on Button

Button size variants already include gap (default/lg: gap-1.5, sm: gap-1). Adding `gap-2` overrides the built-in gap, contradicting the design system's intent.

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `translate/subtitle-translator-main.tsx` | 450 | `className="gap-2"` on size="lg" Button | Remove `gap-2` |
| 2 | `translate/subtitle-translator-main.tsx` | 461 | `className="gap-2"` on size="lg" Button | Remove `gap-2` |
| 3 | `translate/subtitle-translator-main.tsx` | 556 | `className="gap-2"` on default Button | Remove `gap-2` |
| 4 | `translate/subtitle-translator-main.tsx` | 581 | `className="gap-2"` on default Button | Remove `gap-2` |
| 5 | `translate/subtitle-translator-main.tsx` | 593 | `className="gap-2 w-full..."` on default Button | Remove `gap-2` |
| 6 | `translate/subtitle-translator-main.tsx` | 605 | `className="w-full gap-2"` on default Button | Remove `gap-2` |

### Category 8: Replace remaining card-like divs with Card/CardContent

These divs use `bg-card border border-border rounded-lg p-4` (or similar) — duplicating what Card+CardContent provide. Convert to proper Card components.

**8a: Batch components**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `batch/manage-uploads-dialog.tsx` | 118 | `<div className="bg-card border border-border rounded-lg p-4">` | `<Card size="sm"><CardContent>` |

**8b: Project components**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 2 | `project/lists/project-translation-list.tsx` | 120 | `<div className="bg-card border border-border rounded-lg p-4">` | `<Card size="sm"><CardContent>` |
| 3 | `project/lists/project-extraction-list.tsx` | 90 | Same | Same |
| 4 | `project/lists/project-transcription-list.tsx` | 89 | Same | Same |
| 5 | `project/project-item-skeleton.tsx` | 4 | `<div className="border border-border rounded-lg p-3 bg-background">` | `<Card size="sm" className="p-3">` or `<Card size="sm"><CardContent>` with appropriate padding |
| 6 | `project/project-item-list.tsx` | 175 | `className="border border-border rounded-lg p-3 bg-background touch-none"` | `<Card size="sm" className="p-3 touch-none">` (touch-none is custom, needs to stay) |

**8c: Dashboard components**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 7 | `dashboard/project-item.tsx` | 57-60 | `<div className="dark:bg-[#111111] border border-border rounded-lg p-4...">` | `<Card className="dark:bg-[#111111] p-4 hover:ring-primary/50 hover:bg-card/80...">` — Note: `border border-border` removed (Card has ring), `hover:border-primary/50` → `hover:ring-primary/50` |
| ~~8-10~~ | ~~dashboard/features-options.tsx~~ | ~~125,160,193~~ | ~~SKIPPED — custom per-type colors are intentional, keep as div~~ | ~~Skip~~ |

**8e: Auth components**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 13 | `auth/user-settings.tsx` | 60-64 | `<div className="rounded-md overflow-hidden border">` + inner header/content divs | `<Card>` + `<CardHeader>` + `<CardContent>` |
| 14-16 | `auth/user.tsx` | 121, 194, 353 | Three `<div className="rounded-md overflow-hidden border">` + inner header/content | Same Card pattern |

**8f: Transcribe components (next-actions not covered in pass 1)**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 17-18 | `transcribe/transcription-next-actions.tsx` | 30, 50 | `<div className="flex flex-col justify-between p-4 border border-border rounded-md">` | `<Card size="sm" className="flex flex-col justify-between">` + `<CardContent>` |

**8g: Pricing components (remaining files not in pass 1 Cat 8)**

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 19-21 | `pricing/pricing-cards.tsx` | 40, 91, 157 | `<div className="rounded-xl bg-card border border-border overflow-hidden shadow-xs...">` + `<div className="p-6">` | `<Card className="overflow-hidden shadow-xs...">` + `<CardContent>` for standard cards; featured card with `border-2 border-blue-400` stays as special case |
| 22 | `pricing/which-models.tsx` | 14-17 | Outer div with `rounded-xl bg-white dark:bg-gray-900/20 border border-gray-200...` | `<Card className="...">` with token replacement |
| 23 | `pricing/general-features-section.tsx` | 42-50 | Outer div + header div + content div | `<Card>` + `<CardHeader>` + `<CardContent>` |
| 24 | `pricing/transcription-usage.tsx` | 35 | Outer div with `rounded-xl bg-white...p-8` | `<Card>` + `<CardContent>` |
| 25-27 | `pricing/pricing-description.tsx` | 20, 44, 64 | Three outer divs with `rounded-xl bg-white...p-8` | `<Card>` + `<CardContent>` |

**⚠️ SKIP dashboard/features-options.tsx**: User confirmed keeping custom per-feature colors. Do NOT convert to Card — the `dark:bg-[#111111]`, per-type blue/green/purple colors, and `border`-based styling are intentional design choices.

**⚠️ SKIP cloud table wrappers**: These divs use only `px-4` (no vertical padding) as table wrappers. Card adds `py-4` by default, which adds unwanted vertical padding around tables. Keep as divs.

### Category 9: Replace hardcoded colors with design tokens (remaining pricing files)

Previous commit (b703e36) covered: pricing-cards.tsx, credit-usage.tsx, feature-comparison-table.tsx, credit-pack-prices.tsx. These files remain:

| # | File | Instances | Key Replacements |
|---|------|-----------|-----------------|
| 1 | `pricing/which-models.tsx` | `bg-white dark:bg-gray-900/20` → `bg-card`, `border-gray-200 dark:border-gray-800` → `border-border`, `text-gray-900 dark:text-white` → `text-card-foreground` |
| 2 | `pricing/general-features-section.tsx` | Same bg/border/text + `bg-gray-50 dark:bg-gray-900/50` → `bg-muted/50`, `text-gray-800 dark:text-gray-200` → `text-card-foreground`, `text-gray-600 dark:text-gray-400` → `text-muted-foreground` |
| 3 | `pricing/transcription-usage.tsx` | Same bg/border/text + `bg-gray-50 dark:bg-gray-900/30` → `bg-muted/50`, table text colors → tokens, `divide-gray-200 dark:divide-gray-800` → `divide-border`, `hover:bg-gray-50` → `hover:bg-muted/50` |
| 4 | `pricing/pricing-description.tsx` | Same bg/border/text pattern ×3 sections + `text-gray-600 dark:text-gray-400` → `text-muted-foreground` |
| 5 | `pricing/pricing-section.tsx` | `text-gray-600 dark:text-gray-400` → `text-muted-foreground` (2 instances), `text-gray-500 dark:text-gray-400` → `text-muted-foreground` (3 instances in Check/X icons) |
| 6 | `pricing/currency-tabs.tsx` | `text-gray-600 dark:text-gray-400` → `text-muted-foreground`, `bg-gray-200 dark:bg-muted` → `bg-muted` |

### Category 10: Replace hardcoded colors with design tokens (app/auth/dashboard files)

| # | File | Instances | Key Replacements |
|---|------|-----------|-----------------|
| 1 | `app/(landing)/layout.tsx` | `text-gray-900 dark:text-white` → `text-foreground` |
| 2 | `app/error.tsx` | `text-gray-900 dark:text-white` → `text-foreground` |
| 3 | `app/not-found.tsx` | `text-gray-900 dark:text-white` → `text-foreground` |
| 4 | `app/global-error.tsx` | `text-gray-600 dark:text-gray-400` → `text-muted-foreground` |
| 5 | `auth/login.tsx` | `text-red-500` → `text-destructive` |
| 6 | `auth/user.tsx` | `text-red-500` → `text-destructive` (5 instances), `bg-blue-500 hover:bg-blue-600 text-white` → `variant="default"`, `bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-300` → `bg-destructive/10 text-destructive`, `bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300` → `bg-primary/10 text-primary` |
| 7 | `dashboard/project-item.tsx` | Convert card-like div to `<Card>`, `hover:border-primary/50` → `hover:ring-primary/50`. Keep `text-blue-500`/`text-green-500` per-type icon colors |
| ~~8~~ | ~~dashboard/features-options.tsx~~ | ~~SKIPPED — custom per-type colors and border-based styling~~ |

**Note**: Dashboard feature colors and project item type icons use intentional per-category colors. These should NOT be replaced with generic tokens — they serve as visual differentiation.

### Category 11: Misc redundant classes

| # | File | Line | Current | Fix |
|---|------|------|---------|-----|
| 1 | `settings/model-selector.tsx` | 245 | `<div className="bg-popover text-popover-foreground text-sm">` inside HoverCardContent | Remove all 3 classes — HoverCardContent already provides `bg-popover text-sm text-popover-foreground` |
| 2 | `settings/model-selector.tsx` | 228 | `className="w-[260px] text-sm"` on HoverCardContent | Remove `text-sm` — HoverCardContent already provides `text-sm` |
| 3 | `project/project-main.tsx` | 129 | `className="bg-card border border-border p-1 rounded-lg w-fit h-fit flex flex-wrap"` on TabsList | Remove `rounded-lg` (TabsList has `rounded-lg`), remove `w-fit` (TabsList has `w-fit`). Keep `bg-card border border-border p-1 h-fit flex flex-wrap` |
| 4 | `settings/settings-dialogue.tsx` | 184, 209, 227 | `<div className="border border-muted-foreground/20 rounded-md p-4 space-y-6">` | Convert to `<Card size="sm"><CardContent className="space-y-6">` — these are nested sections inside AccordionContent |

## Execution Order

**Workflow**: After completing each category, commit with a descriptive message, then STOP and wait for user review before proceeding.

**Branch setup**: Create `refactor/shadcn-defaults-pass2` from `refactor/shadcn-default-styles-v2` before starting.

1. **Category 1** — Remove redundant `size-8` from `size="icon"` Button (5 instances, 2 files) → **COMMIT, STOP**
2. **Category 2** — Fix contradictory Button sizing overrides (12 instances, 8 files) → **COMMIT, STOP**
3. **Category 3** — Remove redundant `border-border` from outline Button (10 instances, 3 files) → **COMMIT, STOP**
4. **Category 4** — Fix non-functional `hover:border-primary` on Card (4 instances, 4 files) → **COMMIT, STOP**
5. **Category 5** — Remove redundant padding overrides on CardContent/CardFooter (7 instances, 4 files) → **COMMIT, STOP**
6. **Category 6** — Remove redundant mt/mb inside space-y/gap parents (15 instances, 10 files) → **COMMIT, STOP**
7. **Category 7** — Remove redundant `gap-2` on Button (6 instances, 1 file) → **COMMIT, STOP**
8. **Category 8** — Replace remaining card-like divs with Card/CardContent (22 instances, 13 files — features-options.tsx and cloud table wrappers SKIPPED, project-item.tsx included) → **COMMIT, STOP**
9. **Category 9** — Replace hardcoded colors in remaining pricing files (6 files) → **COMMIT, STOP**
10. **Category 10** — Replace hardcoded colors in app/auth/dashboard files (7 files — only features-options.tsx SKIPPED) → **COMMIT, STOP**
11. **Category 11** — Misc redundant classes (4 instances, 3 files) → **COMMIT, STOP**

After all categories: Run `bun typecheck` and `bun lint`, fix issues, amend last commit if needed.

## Decisions

- **dashboard/features-options.tsx**: SKIPPED — keeping custom per-type colors (blue/green/purple) and `dark:bg-[#111111]` border-based styling
- **cloud table wrappers** (uploaded-files.tsx, transcription-history.tsx): SKIPPED — only `px-4` padding, Card would add unwanted `py-4`
- **auth/user.tsx buy button** (`bg-blue-500`): Convert to `variant="default"` — uses `bg-primary` which is the correct design system approach
- **auth/user.tsx status badges** (`bg-red-100 text-red-800` / `bg-green-100 text-green-800`): Replace with `bg-destructive/10 text-destructive` and `bg-primary/10 text-primary`
- **dashboard/project-item.tsx**: Convert card-like div to `<Card>`, fix hover:border→hover:ring, keep per-type icon colors (`text-blue-500`/`text-green-500`)
