# Plan: Move TooltipProvider to app-level providers

## Summary
Remove all local `<TooltipProvider>` wrappers from individual components and add a single `<TooltipProvider>` to the app-level providers tree at `src/contexts/providers.tsx`.

## Step 1: Add TooltipProvider to providers.tsx

- Import `TooltipProvider` from `@/components/ui/tooltip`
- Wrap `<TooltipProvider>` around `<ProvidersClient>` and `<Toaster>` in the return JSX
- Use default `delayDuration` (no custom prop) since individual `<Tooltip>` components can still set their own `delayDuration`
- `providers.tsx` is a server component but can render client components imported from `'use client'` modules — this works in Next.js

## Step 2: Remove local TooltipProvider wrappers (9 files)

For each file: remove `<TooltipProvider>` and `</TooltipProvider>` wrapping tags, remove `TooltipProvider` from the import statement, and **move any `delayDuration` from `TooltipProvider` to `Tooltip`** (since `Tooltip` accepts `delayDuration` as its own prop).

| # | File | Lines | Notes |
|---|------|-------|-------|
| 1 | `src/components/ui/sidebar.tsx` | 529, 539 | Remove wrapper (no delayDuration); keep `<Tooltip>` |
| 2 | `src/components/settings/start-index-input.tsx` | 75, 93 | Remove wrapper (no delayDuration); keep `<Tooltip delayDuration={10}>` |
| 3 | `src/components/settings/advanced-reasoning-switch.tsx` | 18, 29 | Remove wrapper (no delayDuration); keep `<Tooltip delayDuration={10}>` |
| 4 | `src/components/pricing/feature-comparison-table.tsx` | 82, 91 | Remove wrapper; move `delayDuration={50}` from `<TooltipProvider>` → `<Tooltip delayDuration={50}>` |
| 5 | `src/components/pricing/payment-options-dialog.tsx` | 317, 329 | Remove wrapper; move `delayDuration={50}` from `<TooltipProvider>` → `<Tooltip delayDuration={50}>` |
| 6 | `src/components/pricing/credit-usage.tsx` | 88, 97 and 100, 109 | Remove **two** wrappers (no delayDuration on provider); keep `<Tooltip delayDuration={0}>` on each |
| 7 | `src/components/batch/sortable-batch-transcription-file.tsx` | 114, 125 and 138, 147 | Remove **two** wrappers; move `delayDuration={50}` from each `<TooltipProvider>` → each `<Tooltip delayDuration={50}>` |
| 8 | `src/components/batch/sortable-batch-file.tsx` | 117, 126 | Remove wrapper; move `delayDuration={50}` from `<TooltipProvider>` → `<Tooltip delayDuration={50}>` |
| 9 | `src/components/ui-custom/coming-soon-tooltip-wrapper.tsx` | 13, 24 | Remove wrapper; move `delayDuration={50}` from `<TooltipProvider>` → `<Tooltip delayDuration={50}>` |

## Step 3: Clean up unused imports

After removing the JSX usage, remove `TooltipProvider` from the import statement in each file. Files that still use `Tooltip`, `TooltipContent`, `TooltipTrigger` keep those imports.

## Step 4: Verify

Run `bun typecheck` and `bun lint` to confirm no errors.
